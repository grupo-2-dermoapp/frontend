import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uniandes.dermoapp',
  appName: 'DermoApp',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
